INSERT TodoEntry {
  sessionId := <str>$sessionId,
  position := <int32>$position,
  content := <str>$content,
  status := <str>$status,
  priority := <str>$priority,
  createdAt := <int64>$ts,
  updatedAt := <int64>$ts,
}
